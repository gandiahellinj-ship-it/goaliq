# GoalIQ — Decisiones cerradas

> Fuente de verdad del proyecto. Una decisión no está tomada hasta que está aquí.
> Regla de mantenimiento: al cerrar cada fase o decisión, añadir UNA línea con fecha.
> Claude Code: lee este archivo al inicio de cada sesión y actualízalo al cerrar trabajo.

## Identidad y diseño (interfaz /vision)
- **2026-07-05** — Dirección visual de la app interna: paleta beige/dorada sobre fondo crema (la identidad cian queda para la intro cinematográfica; el desajuste intro↔app sigue abierto, ver PENDIENTES).
- **2026-07-05** — Layout: viewport fijo de 3 zonas (65% contenido / nav 5 botones circulares / ~26% panel contextual). Sin scroll vertical en ninguna zona. Sustituye a los "5 pisos" con flechas.
- **2026-07-06** — Tipografía: Barlow Condensed (titulares) + Plus Jakarta Sans (cuerpo). No añadir fuentes nuevas.
- **2026-07-06** — Tokens de color: los valores existentes en `index.css` son la fuente de verdad (#F4F4F4, #FAFAFA, #BA9D79, #888888, #E5E5E5, #1A1A1A). Añadidos: `--color-brand-accent-soft` #EFE8DC y `--color-brand-red` #E5484D. Renombrado `--color-brand-cyan` → `--color-brand-accent` (sin cambio de valor).
- **2026-07-06** — HOME: itinerario del día tipo checklist con línea de progreso; filas de SUPLEMENTO compactas (media altura, una línea). Zona contextual: SIGUIENTE tarea + contador.
- **2026-07-07** — COMIDAS (rediseño aprobado sobre la Fase 2): zona superior = plato seleccionado protagonista (imagen + ingredientes + preparación + bloque educativo + botón marcar); franja fina única de kcal+macros. Zona inferior = carrusel horizontal de platos, seleccionado más grande, sincronizado con la zona superior.
- **2026-07-07** — Estilo estándar de imagen de plato: fotografía cenital fotorrealista, luz de estudio suave, generada sobre fondo blanco puro, 1:1, recortada a PNG transparente por código. Miniaturas del mismo estilo en el carrusel.
- **2026-07-08** — ENTRENOS (rediseño 3b, mismo patrón que COMIDAS 2b): zona superior = ejercicio seleccionado protagonista (nombre + clip en bucle grande + series×reps + tip en bloque beige + botón marcar) y franja fina única (duración · kcal · % hecho). Zona inferior = carrusel horizontal de ejercicios (miniatura + nº + sets×reps + check), seleccionado algo mayor, con botón «Entreno completado» (marca todos). Clips = `<video>` autoplay/muted/loop/playsinline con placeholder animado neutro de fallback (nunca hueco roto).
- **2026-07-08** — PROGRESO y AJUSTES migradas al layout 3 zonas. PROGRESO: gráfica de peso + meta + 4 métricas (arriba); distancia a la meta + variación semanal (contextual). AJUSTES: perfil/idioma/tema/Mi perfil-Privacidad-Términos/cerrar sesión REALES reestilados, logout en rojo tokenizado (`--color-brand-red`); versión app + nota de privacidad (contextual). Con esto las 5 pestañas del layout están migradas; solo queda la Fase 6 (limpieza + escritorio).
- **2026-07-08** — PROGRESO rediseño 4b «Tu paisaje muscular»: el protagonista es un paisaje de montañas superpuestas (una por grupo muscular), altura = series de la semana, tono arena del más trabajado (`--color-brand-accent`) al menos trabajado, picos unidos por línea punteada; etiquetas grupo+series debajo; franja fina «Semana N · X series»; bloque educativo; dos mini-tarjetas de peso (tendencia + meta). Desaparece la gráfica de evolución de peso de la zona superior; la zona contextual (distancia a meta + variación) no cambia.
- **2026-07-09** — Escritorio (≥640px): /vision se presenta como una columna de 430px centrada con esquinas redondeadas + sombra sobre lienzo neutro cálido (#E4DFD6); en móvil ocupa el ancho completo sin marco. Revisión de escritorio de la Fase 6.
- **2026-07-09** — PROGRESO 4b v2 (DEFINITIVO, sustituye al 4b anterior): paisaje en tarjeta blanca con las 6 montañas SIEMPRE completas a 390px; selector de 6 chips (activo en píldora oscura, scroll horizontal si no caben). Seleccionar grupo (chip o montaña) lo resalta (montaña oscura, demás al 35%) y actualiza la ZONA CONTEXTUAL con «Análisis detallado» del grupo: barras por subgrupo con «N series (X%)» + «Consejo del entrenador» verbatim (clamp adaptativo por nº de subgrupos para caber sin scroll). El bloque insight superior es la frase FIJA de equilibrio. 2 mini-tarjetas (Peso 82.1 ↘0.3 · Meta 78, faltan 4.1); NO hay tarjeta oscura «Hacia la meta» y el VERDE está prohibido en la pestaña (solo tokens beige). Datos de subgrupos y textos: `design/referencia-progreso.txt`.
- **2026-07-08** — Fix: páginas legales (Privacidad/Términos) eran ilegibles en tema claro (blanco sobre blanco) por `prose-invert`; se mapeó `.prose` a los tokens `--giq` del tema. Legible en claro (/vision) y oscuro (/dashboard).
- **2026-07-07** — Prompt maestro de imágenes de plato fijado en [`docs/PROMPT_PLATOS.md`](PROMPT_PLATOS.md): fuente de verdad. En producción se copia al servidor; cualquier cambio del prompt se hace AHÍ primero.
- **2026-07-07** — Regla de bebidas: para bebidas se sustituye la frase del bol por el recipiente propio (vaso), según la variante «Bebidas» del prompt maestro.
- **2026-07-07** — El plan genera por plato un campo `descripcion_imagen` en inglés (ingredientes visibles + cocinado) que rellena `[PLATO]`/`[BEBIDA]`; el nombre en español es para la UI y clave de caché.
- **2026-07-08** — Recorte de imágenes de plato con **rembg** (segmentación IA), no flood-fill por color: elimina también la sombra de contacto que el generador pinta bajo el bol. La sombra de profundidad la pone la app por CSS (`drop-shadow`); las imágenes llegan sin sombra. El prompt maestro lo refuerza («flat even lighting with no contact shadow»). ⚠️ **SUPERADA el 25/07/2026** (ver entrada de esa fecha): rembg (Python) se descartó a favor de `@imgly/background-removal-node` (Node), porque el servidor es Node en Replit y añadir Python complicaba el despliegue. Misma finalidad (PNG transparente sin sombra), otro motor. El código de producción usa @imgly.
- **2026-07-08** — `descripcion_imagen` se construye DESDE los ingredientes del plato: todos los VISIBLES en el resultado deben aparecer; se omiten condimentos/no visibles (sal, aceite, ajo disuelto). Detalle en [`docs/PROMPT_PLATOS.md`](PROMPT_PLATOS.md).
- **2026-07-08** — Clave de caché de imágenes en producción = nombre normalizado + hash de ingredientes visibles (recetas distintas con mismo nombre → fotos distintas; misma receta → foto compartida).

## Producto y datos
- **2026-07-06** — Migración visual y cableado de datos son proyectos SEPARADOS. La migración usa datos mock (src/data.ts); solo suplementos y ajustes son reales.
- **2026-07-06** — Chips de HOME (fase de datos reales): Cumplimiento hoy % + Proteína hoy (g) + Volumen semanal (series). Se eliminan Movimiento/Pasos/Agua (inviables en PWA). Fórmula inicial del % (objetivo ganar músculo): proteína 40 / entreno 30 / comidas 20 / suplementos 10 — a calibrar.
- **2026-07-07** — Imágenes de platos en producción: se generan con IA tras crear el plan, en segundo plano (nunca bloquean la generación), con caché por nombre de plato NORMALIZADO en Supabase Storage, compartida entre usuarios. Fallback: círculo con iniciales.
- **2026-07-07** — Proveedores de IA: Claude (Anthropic) en exclusiva para texto y visión. EXCEPCIÓN única: generación de imágenes con Gemini 2.5 Flash Image / Nano Banana (~$0,039/imagen), porque la API de Anthropic no genera imágenes. NO usar Imagen 4 (se apaga el 17/08/2026).
- **2026-06 (heredadas, vigentes)** — Lenguaje legal "orientativo", nunca "personalizado" ni promesas de resultados. Comidas del plan = ingredientes + preparación generados por Claude Haiku en el onboarding (paso 7/7 → /api/consent → /api/plan/generate).

## Precio y negocio
- **2026-07-07** — Precio mensual: 19,90 €/mes (techo del mercado; no subir). Añadir plan anual ~99-119 €/año como ancla de compra. Precio fundador para la beta (p. ej. 9,90 €/mes o anual 79 € primeros 100).
- **2026-07-07** — Tope de regeneraciones de plan por usuario obligatorio (evita coste de IA descontrolado). Regeneraciones no urgentes → Batch API (50% dto., hasta 24 h).

## Flujo de trabajo
- **2026-07-06** — Pauta permanente con Claude Code: commit + push a origin al cerrar CADA fase. Nunca acumular trabajo sin subir.
- **2026-07-06** — Trabajar siempre en rama (actual: feature/layout-3-zonas). No fusionar a main hasta que las 5 pestañas estén aprobadas. Main = lo que Replit publica.
- **2026-07-22** — Regla suprema en CLAUDE.md: el código implementado manda sobre cualquier documento; ante contradicción se corrige el documento, no el código.
- **2026-07-22** — Seguridad: los 3 endpoints de IA (`/api/diets/generate`, `/api/diets/visualize`, `/api/meals/validate`) exigen sesión y tienen límite de uso (3/min, 10/hora por usuario); `/api/recipes/random` capado a 30/min por IP. Nadie sin cuenta puede gastar crédito de IA.
- **2026-07-22** — `/cierre` hace push SOLO a la rama activa; nunca fusiona ramas. La fusión es una tarea aparte que decide José. La fusión de feature/layout-3-zonas queda condicionada a verificar antes la instalación en Linux (punto 3 de la auditoría).
- **2026-07-22** — Claude Code nunca pide a José pegar claves o contraseñas en el chat; si una prueba las necesita, se propone una alternativa que no las exponga.
- **2026-07-22** — Lo cinematográfico se aparca en `docs/ARCHIVO_IDEAS.md` (sin borrarlo); no se retoma sin que José lo pida.
- **2026-07-22** — Instalación multiplataforma: eliminados los vetos de binarios de `pnpm-workspace.yaml` y los parches win32; `packageManager` fijado a pnpm@10.33.2. Chequeo permanente de GitHub Actions ("Linux check") en TODAS las ramas: si está en rojo, no se fusiona ni se publica.
- **2026-07-22** — Orden de trabajo tras la auditoría: la PRÓXIMA tarea es la fusión de feature/layout-3-zonas a main (con plan previo: verificación del despliegue en Replit + plan de marcha atrás). Los puntos 4 (dependencias) y 5 (limpieza) de la auditoría se hacen DESPUÉS de la fusión, una sola vez sobre el proyecto unificado.
- **2026-07-23** — **LA FUSIÓN ESTÁ EN PRODUCCIÓN** desde el 23/07/2026: https://nutrition-tracker-pwa.replit.app — portada beige + /vision (5 pestañas) publicadas junto a la app antigua; datos de usuarios intactos; candado de IA verificado en producción (401 sin sesión). Checklist visual completo aprobado por José.
- **2026-07-23** — **Mecanismo REAL de despliegue** (descubierto y establecido hoy; sustituye a la creencia "sincroniza vía GitHub"): Replit nunca estuvo conectado por Git — era copia manual. Ahora sí está conectado: el flujo es **GitHub `main` → `git pull github main` en la Shell de Replit → Republish en el panel Publishing**. La rama `master` interna de Replit queda como reliquia/red de seguridad; el ZIP del 22/07 (con `exports/` de la migración heliumdb, datos de usuarios — NO subir a GitHub) es la copia histórica.
- **2026-07-23** — **Regla fija: en Replit no se edita código NUNCA, y el Agente de Replit no se usa.** Replit es solo hosting: pull + publish. Todo el desarrollo pasa por este repositorio y sus verificaciones (CI Linux).
- **2026-07-23** — El asistente de Stripe de Replit (paso 3: conectar la cuenta REAL) NO se completa hasta la Mejora 13 (activar pagos). La conexión sandbox se eliminó del panel de Integraciones; el código usa `STRIPE_SECRET_KEY` de Secrets (sk_test) y el conector era solo camino de reserva.
- **2026-07-23** — Build de Replit: dos ajustes en `.npmrc` para su máquina de build (límites de concurrencia + desactivar la auto-instalación de pnpm, que moría por falta de hilos). La versión de pnpm sigue fijada vía corepack en CI y en local.
- **2026-07-23** — Hoja de ruta hacia la meta (José): (1) modal de login a beige, (2) cablear /vision con datos reales, (3) cambio de puerta — /vision raíz, app antigua retirada.
- **2026-07-23** — Comportamiento documentado: Replit crea un commit VACÍO "Published your App" (autor: Replit Agent) en cada publicación, que hace divergir su rama y chocar el siguiente `git pull`. Procedimiento estándar de publicación desde ahora: `git fetch github && git reset --hard github/main` en la Shell (descarta esos commits de trámite de serie; sin riesgo porque en Replit nunca hay trabajo propio). Actualizado en CLAUDE.md.
- **2026-07-23** — Paso 1 de la hoja de ruta CERRADO: modal de login en beige, en producción. La puerta de entrada ya es coherente con la portada /vision.
- **2026-07-24** — DISEÑO DEFINITIVO del bucle diario (detalle en FLUJO_DIARIO.md §4): registro por niveles en cada pestaña (entreno con botón "Registrar" y hora configurable); progreso semanal/mensual automático con CRÉDITO PARCIAL (un día al 90% puntúa 90%); "Registrar día" = LA RACHA, estricto (todo registrado, entreno/descanso incluido); ventana de gracia hasta las 12:00 del día siguiente; racha oculta en la interfaz hasta que el sistema exista. Cierra la decisión abierta "estricto vs. crédito parcial": parcial en progreso, estricto en racha.
- **2026-07-24** — El chip "Cumplimiento hoy %" se APLAZA al bucle diario (su fórmula necesita objetivo de proteína y registros de suplementos que aún no existen). El HOME real muestra mientras tanto los 2 chips con datos reales: Proteína hoy (g) y Volumen semanal (series).
- **2026-07-24** — Fases A-D del cableado de /vision COMPLETADAS Y VERIFICADAS en un día: las 5 pestañas con datos reales (COMIDAS del plan, ENTRENOS con día de descanso, HOME con itinerario unificado y checks compartidos, PROGRESO con paisaje real y peso). Convenciones nuevas: nada se inventa (sin fuente real → se oculta o "—"), frase de equilibrio calculada, verificación local contra producción vía proxy de desarrollo (API_PROXY_TARGET).
- **2026-07-25** — Onboarding restilado a beige Y EN PRODUCCIÓN: token acotado `--color-brand-warn` (ámbar, solo avisos de salud), iconos de línea lucide en vez de emojis (regla añadida a CLAUDE.md), regla de contraste documentada (títulos/datos en tinta, secundarios en gris, solo placeholders suaves). El slider de ritmo (3 opciones) → 3 tarjetas seleccionables con etiqueta + dato. Con esto la puerta de entrada completa (portada → login → onboarding) es 100% beige. Pendiente menor: GenerationOverlay compartido sigue oscuro (se resuelve en el cambio de puerta).
- **2026-07-25** — Nueva tarea funcional definida por José: rediseño del paso 5 del onboarding (modalidad de entreno, ver PENDIENTES) — selección múltiple de cómo entrenar + equipamiento; la IA combina objetivo + modalidad + equipamiento priorizando el objetivo. Requiere plan de punta a punta (perfil + prompt + usuarios existentes) antes de implementar.
- **2026-07-25** — Imágenes de plato, Fase 0 COMPLETADA Y APROBADA (6 imágenes de prueba, ~0,24 €). Decisiones cerradas: (1) recorte a PNG transparente con librería Node `@imgly/background-removal-node` (NO rembg/Python) — validado, ~2,5 s/imagen, la versión Node NO acepta data-URLs (pasar Blob). (2) Generación BAJO DEMANDA al ver el plato (no en segundo plano tras crear el plan) — más robusto en autoscale y más barato; refina la decisión del 07/07. Caché compartida entre usuarios por nombre normalizado + hash de ingredientes visibles. (3) Prompt maestro: fondo GRIS CÁLIDO medio con plato blanco (no blanco sobre blanco) — sin contraste el recortador se comía el borde del plato y los elementos finos; validado con el plato difícil (espárragos/eneldo). Descartada la variante de plato crema. (4) Secreto en Replit = `GOOGLE_GEMINI_API_KEY` (no GEMINI_API_KEY); corregido también el endpoint `/api/diets/visualize` que leía el nombre equivocado y fallaba en silencio. (5) Facturación Google Cloud = prepago 10 € SIN recarga automática = límite duro de gasto.
- **2026-07-25** — Frontend de imágenes de plato: mientras se genera, se muestra el círculo de iniciales (ya existente); cuando la foto llega, aparece con FUNDIDO suave (fade-in), sin spinners agresivos ni saltos bruscos. Fallback permanente si la generación falla = círculo de iniciales.
- **2026-07-27** — **SE ELIMINA EL RECORTE de las imágenes de plato** (cambio de enfoque de José, tras encontrar la causa raíz). Motivo: `@imgly/background-removal-node` daba `ENOENT` en el Deployment de Replit (le faltaban los archivos de modelo en node_modules; el paquete está completo en local pero el deploy no los incluye) y es ML nativo pesado/frágil (ya dio 2 bugs: el `external` de esbuild y este). En vez de recortar, se pide a Gemini el fondo YA del color de la tarjeta de /vision (`--color-brand-bg` = **#F4F4F4**, verificado en index.css). Consecuencias: (1) fuera `@imgly/background-removal-node`, `onnxruntime-node` y `sharp` del servidor (external de esbuild, package.json, onlyBuiltDependencies); (2) se cancela el worker de GitHub Actions que se barajaba; (3) la generación vuelve a ser EN EL SERVIDOR, bajo demanda (~5 s sin recorte, antes ~30 s). Hallazgos del análisis: **/vision es hoy SOLO tema claro** (los tokens `.goaliq-vision` son fijos, sin variante oscura; el toggle de AJUSTES no cambia /vision — incoherencia preexistente a anotar). La desviación de color de Gemini se tolera por CSS (mask-image radial de fundido de bordes) o mostrando la foto como tarjeta redondeada (robusta también si algún día hay modo oscuro). Pendiente: validar el color con 2 imágenes de prueba (ronda 3) antes de implementar; limpiar filas `failed` de dish_images.
- **2026-07-24** — Fase E del cableado COMPLETA: (1) cada pestaña distingue ERROR (fallo de red/servidor → "No hemos podido cargar / Reintentar") de VACÍO (sin plan) — regla de "pensar de punta a punta"; excepción documentada: ENTRENOS no distingue error por limitación heredada del gancho antiguo (se traga los fallos), no se toca la app antigua. (2) `/vision` ahora exige sesión (puerta de login propia, beige); sin sesión no se muestran las tripas. (3) VisionApp ya NO importa la maqueta data.ts. Verificado por José los 3 casos en local (sin sesión, recién registrado, error de red con recuperación).
- **2026-07-24** — Arreglo del proxy de desarrollo (vite.config): al reenviar a producción se elimina la cabecera Origin, porque el CORS de producción (modo prod) rechazaba el Origin "localhost" y devolvía 500 en las POST. Solo afecta al proxy local; producción no cambia. Sin esto no se podían probar registros/consentimientos en local contra producción.
