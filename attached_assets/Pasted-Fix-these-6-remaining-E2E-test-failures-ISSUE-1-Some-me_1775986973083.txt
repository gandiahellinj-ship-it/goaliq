Fix these 6 remaining E2E test failures:

## ISSUE 1: Some meals have fewer than 2 ingredients (TEST 4)
Warning: "Some meals have fewer than 2 ingredients"
Add this to the meal generation prompt:
"CRITICAL: Every single meal MUST have at least 3 ingredients. Never return a meal with fewer than 3 ingredients. If a meal would have fewer ingredients, add basic items like olive oil, salt, or vegetables."

## ISSUE 2: GET /meals returns 404 (TEST 5)
The GET /meals endpoint is returning 404.
Check if the route exists: GET /api/meals
If it doesn't exist as a GET endpoint, create it:
GET /api/meals — returns the current week's meal plan for the authenticated user
Query meal_plans table for user_id = current user AND week_start = current week Monday
Return array of meal objects

## ISSUE 3: Workout plan generation still fails (TEST 6)
Error: POST /api/workouts → 500: Workout plan generated but could not be saved
The workout is being generated but fails to save.
Check the workout_plans table insert:
1. Log the exact error when saving to workout_plans
2. Check if duration_minutes column exists (migration may not have run)
3. Check if the insert data matches the table schema exactly
4. Fix the save logic

## ISSUE 4: Calendar mark complete fails (TEST 9)
Error: POST /api/calendar/complete → 500: Failed to save calendar event
The calendar insert is still failing.
Debug:
1. Log the exact PostgreSQL error
2. Check if calendar_events table exists in local PostgreSQL (not just Supabase)
3. The table might need to be in local PostgreSQL instead of Supabase
4. Try creating it in local PostgreSQL at startup if it doesn't exist there

## ISSUE 5: Streak = 0 (TEST 12)
Cascade from Issue 4. Will fix automatically when calendar works.

## ISSUE 6: Shopping list shows 0 categories (TEST 8)
Warning: "69 unique ingredients across 0 categories"
The ingredients are being found but not categorized.
Fix the category detection to work with Spanish ingredient names:
The categorization function needs Spanish keywords:
- Verduras: espinacas, lechuga, tomate, pepino, zanahoria, cebolla, pimiento, brócoli, calabacín, champiñones, ajo, apio
- Proteína: pollo, pechuga, ternera, salmón, atún, huevos, gambas, lentejas, garbanzos, tofu, cerdo
- Lácteos: leche, yogur, queso, mantequilla, nata
- Carbohidratos: arroz, pasta, pan, avena, quinoa, patata, boniato
- Grasas: aceite, aguacate, nueces, almendras, semillas
- Frutas: manzana, plátano, naranja, fresas, uvas, limón
- Otros: todo lo demás

Fix all 6 issues and report back.