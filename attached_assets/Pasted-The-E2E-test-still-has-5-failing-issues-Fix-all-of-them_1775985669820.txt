The E2E test still has 5 failing issues. Fix all of them:

## ISSUE 1: Meal plan cannot be inserted into database (TEST 5)
Error: "Could not insert into meal_plans"
The AI generates 21 meals correctly but they fail to save to Supabase meal_plans table.
Check the insert logic in /api/meals:
1. Verify the meal_plans table schema matches what's being inserted
2. Check if there's a RLS policy blocking the insert
3. Add detailed error logging to show the exact Supabase error
4. Fix the insert so meals save correctly

## ISSUE 2: Workout plan generation fails (TEST 6)
Error: POST /api/workouts → 500: Workout plan generation failed
The workout generation is still failing.
Apply the same jsonrepair fix that was applied to meals:
1. Make sure jsonrepair is imported in the workout generation code
2. Use the same 4-step JSON recovery pipeline for workout responses
3. Test with a real profile and verify 3 workouts are returned

## ISSUE 3: Calendar mark complete fails (TEST 9)
Error: POST /api/calendar/complete → 500: unknown
Still failing after the previous fix.
Debug this specifically:
1. Log the exact request body being received
2. Log the exact Supabase error when inserting
3. Check if calendar_events table exists in Supabase (it was just created)
4. Check RLS policies on calendar_events table
5. Fix whatever is blocking the insert

## ISSUE 4: Streak = 0 (TEST 12)
The streak is still 0 after marking workout days complete.
This is likely a cascade from Issue 3 (calendar not saving).
Once Issue 3 is fixed, verify the streak calculation reads from calendar_events correctly.

## ISSUE 5: Language check warning (TESTS 4 & 14)
Warning: "tuesday/lunch: Pechuga de pollo con mayonesa y ensalada de lechuga"
The meal is in Spanish but the day_name field is storing "tuesday" in English instead of "martes".
Fix the meal generation prompt to return day names in Spanish:
- monday → lunes
- tuesday → martes  
- wednesday → miércoles
- thursday → jueves
- friday → viernes
- saturday → sábado
- sunday → domingo

Add to the meal generation prompt:
"IMPORTANT: Use Spanish day names: lunes, martes, miércoles, jueves, viernes, sábado, domingo"

After fixing, run the E2E test again to verify all 5 issues are resolved.