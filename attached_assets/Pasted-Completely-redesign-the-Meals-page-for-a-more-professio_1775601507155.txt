Completely redesign the Meals page for a more professional and premium look.

## Meal card redesign

Each meal card should have:

HEADER:
- Meal type label (DESAYUNO/COMIDA/CENA) in #AAFF45, bold, 11px, uppercase, letter-spacing wide
- Meal name in white, bold, 22px
- Two small info pills on the right side of the header:
  * ⏱ prep time (use prep_time_minutes field from AI, or estimate: breakfast=10min, lunch=15min, dinner=25min)
  * 🔥 calories (use calories_approx field from AI)
  * Style: background #2A2A2A, text #A0A0A0, border-radius 20px, padding 4px 10px, font-size 12px

INGREDIENTS SECTION:
- Label "INGREDIENTES Y PORCIONES" stays but make it more subtle
- Each ingredient row: ingredient name on left, quantity pill on right
- Quantity pill: background #1A1A1A, border 1px solid #2A2A2A, border-radius 6px, text white, padding 2px 8px
- Remove the colored dots — they add noise without value
- Add a subtle hover effect on each ingredient row: background #1E1E1E

DISTRIBUTION SECTION:
- Keep the donut chart but make it slightly smaller (120px)
- Remove the "Guía del plato" tags completely — they are confusing and redundant
- Keep only the chart + legend

EXPAND/COLLAPSE:
- Each meal card is collapsed by default showing only the header with name + time + calories
- Click anywhere on the header to expand and see ingredients + chart
- Add a subtle chevron ▼ on the right that rotates when expanded
- Smooth animation on expand/collapse (300ms ease)

CARD STYLE:
- Background: #151515
- Border: 1px solid #222222
- Border-radius: 12px
- Add a subtle left border accent (3px) in the meal type color:
  * DESAYUNO: #AAFF45 (lime)
  * COMIDA: #FFB347 (orange)  
  * CENA: #7B8CDE (purple/blue)
- Hover: border color brightens slightly

## Day selector tabs
- Make the selected day tab more prominent: background #AAFF45, text #0A0A0A, font-weight bold
- Unselected days: background #1A1A1A, text #555555
- Today's day: add a small lime green dot indicator above the tab

## Page header
- Add a subtitle below "MENÚ SEMANAL": 
  "Tu plan personalizado · {diet_type} · {goal}" in #555555, 14px
  Example: "Tu plan personalizado · Omnívoro · Mantener peso"

## Do not change any functionality, only the visual design.